[project]
name = "contextcull"
version = "1.0.0"
description = "ContextCull: Deterministic Context Compiler & Pre-Processor for Frontier LLMs"
readme = "README.md"
authors = [
    { name = "Mayur Virkar", email = "mayurvirkar@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "ahocorasick-rs>=1.0.3",
    "defusedxml>=0.7.1",
    "lingua-language-detector>=2.0.2",
    "markdown-it-py>=4.2.0",
    "numpy>=2.5.3",
    "orjson>=3.12.0",
    "pypdf>=6.19.0",
    "scikit-learn>=1.9.1",
    "scipy>=1.18.1",
    "sparse-dot-topn>=1.2.0",
    "tiktoken>=0.14.0",
    "typer-slim>=0.24.0",
]

[project.scripts]
contextcull = "contextcull.cli:main"
cull = "contextcull.cli:main"

[project.urls]
Homepage = "https://github.com/MayurVirkar/ContextCull"
Repository = "https://github.com/MayurVirkar/ContextCull.git"
Issues = "https://github.com/MayurVirkar/ContextCull/issues"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "bandit>=1.9.4",
    "coverage>=7.16.1",
    "hypothesis>=6.168.0",
    "mutmut>=3.8.0",
    "pip-audit>=2.10.1",
    "pyright>=1.1.414",
    "pytest>=9.1.1",
    "pytest-cov>=6.0.0",
    "ruff>=0.16.8",
]
bench = [
    "nltk>=3.9.1",
    "sumy>=0.13.0",
]

[tool.ruff]
line-length = 100
target-version = "py313"
exclude = [".venv", "examples"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
ignore = [
    "B008",  # Typer argument defaults
    "E501",  # Line length
    "B905",  # zip strict parameter
]

[tool.pyright]
include = ["src", "tests", "bench"]
exclude = [".venv"]
venvPath = "."
venv = ".venv"
pythonVersion = "3.13"
reportMissingTypeStubs = false

[tool.bandit]
exclude_dirs = [".venv", "tests", "bench", "scratch"]
skips = [
    "B105",  # hardcoded_password_string: false positive on "token" dictionary key
    "B110",  # try_except_pass: deliberate fallback in parser detection
    "B112",  # try_except_continue: deliberate resilient loop in PDF page parsing
]

[tool.pytest.ini_options]
pythonpath = ["src", "../src"]
testpaths = ["tests"]

[tool.mutmut]
source_paths = ["src/contextcull/"]
runner = "python -m pytest tests/unit/ -x -q"

[tool.coverage.run]
source = ["src/contextcull"]
omit = ["src/contextcull/cli.py"]

[tool.coverage.report]
fail_under = 80
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
]
